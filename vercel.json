{
    "version": 2,
    "builds": [{
        "src": "public/index.php",
        "use": "@vercel/php",
        "config": {
            "installCommand": "curl -sS https://getcomposer.org/installer | php && php composer.phar install"
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "public/index.php"
    }]
}
