{
    "version": 2,
    "builds": [{
        "src": "api/index.php",
        "use": "@vercel/php",
        "config": {
            "composer": true
        }
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "api/index.php"
    }]
}
