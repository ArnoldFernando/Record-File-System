{
    "version": 2,
    "builds": [{
        "src": "public/index.php",
        "use": "@vercel/php"
    }],
    "routes": [{
        "src": "/(.*)",
        "dest": "public/index.php"
    }],
    "installCommand": "curl -s https://getcomposer.org/installer | php && php composer.phar install --optimize-autoloader --no-dev",
    "buildCommand": "php artisan key:generate && php artisan optimize",
    "outputDirectory": "public"
}
